# ⚠ Veraltet — nicht weiterentwickeln

Dieses Verzeichnis enthält den Vorgänger-Entwurf des Update-Dienstes (`update-service/`).

Die neue Architektur sieht `toob-update` als eigenständiges Hetzner-Projekt vor
(siehe `plan.md` §2 Projekt-Topologie, §8 Ingress und Edge).
Der Code hier dient nur noch als Referenz für die Migration und wird nach deren
Abschluss entfernt.

Neue Arbeit am Update-Dienst findet **nicht** in `svc/` statt.
